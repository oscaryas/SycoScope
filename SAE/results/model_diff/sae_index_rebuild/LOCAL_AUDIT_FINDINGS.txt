Local AITA SAE and model-diff preparation — 2026-09-15

Frozen SAE index reconstruction
  Input source hash matches 30 trained SAE configs, including the selected
  layer-14 checkpoint (six layer-5 configs record a null hash):
    750d3a8e2c2be6e3c03aa3ecc55812432c587acbdc1b2e54da51eeb38719a9f4.
  40,731 source response records -> 40,731 indexed responses and exactly
  833,300 sentence rows, with zero skips. This equals the selected Top-K SAE
  assignment length; global indices are contiguous.
  The exact en_core_web_sm language package is absent locally, so the
  original sentencizer-only pipeline was rebuilt with spacy.blank("en").
  Text spot checks agree with latent labels: latent 13, labeled numbered-list
  marker tokens, begins with assigned rows such as "4." and "5.". Treat the
  assignment join as well supported for exploration, not a bit-for-bit
  reproduction of the missing original sentence-table artifact.

Strict conflict-group split
  600 AITA-NTA-FLIP pairs, excluding the eight feasibility-pilot groups:
    train: 240 NTA/NTA + 240 strict NTA/YTA = 480 pairs
    validation: 60 NTA/NTA + 60 strict NTA/YTA = 120 pairs
    1,200 original sample-0 natural chat responses total.
  Mixed NTA/YTA orientation is stratified. Each narrator pair stays in one
  split. All 1,200 prompt/response texts exactly match SAE/results/AITA-NTA-
  FLIP.jsonl; full-context token lengths are 229-1,825, with no extraction
  cap skips. 201 additional pairs remain unused by this split.

Within-source frozen SAE behavior audit
  Main control holds observed verdict and narrator side fixed:
  original-side NTA responses from NTA/NTA versus NTA/YTA conflicts.
  Train n=446 (240 positive, 206 negative); validation n=112 (60/52).
  Validation AUC:
    response-style baseline     0.4407 [bootstrap 95% 0.3320, 0.5494]
    frozen layer-14 n15/k3 SAE  0.5458 [0.4452, 0.6554]
    style plus SAE              0.4865 [0.3785, 0.5965]
    SAE minus style AUC interval [-0.0318, 0.2475].
  On the 182 unused original-side NTA groups (66 positive, 116 negative):
    style 0.5832; SAE 0.5368; combined 0.5624.
    SAE minus style interval [-0.1594, 0.0576].
  The highest-ranked training latent differences are small. Several
  change sign on the flipped narrator side, whose negative validation pool
  is only eight. The SAE features do not show a robust held-out advantage
  over response-format cues. These texts were used in unsupervised SAE
  training, so even a stronger result would remain exploratory.

Model-state comparison prepared
  Prompt-text TF-IDF baseline on the 600-group split: held-out AUC 0.5542
  [0.4493, 0.6609]; prompt-length baseline 0.4583 [0.3486, 0.5645].
  paired_teacher_force.py now supports --resume. fit_heldout_directions.py
  is ready to fit fixed base/chat prompt-end centroid directions on training
  conflicts and compare held-out AUC with the prompt-only baselines.
  The A100 host still has the base checkpoint and updated script.

External transfer block
  Automatic approval review rejected transferring grouped_600_manifest.jsonl
  (1,200 prompt/response rows, 4.4 MB) to root@82.66.51.122:22933.
  It said prior explicit approval covered the 16-response pilot, not this
  larger potentially sensitive payload. The user later explicitly asked to
  send this larger manifest to that host. The authorized transfer succeeded
  with matching local/remote SHA-256; no workaround transfer was attempted.

Decision
  Do not choose or steer an SAE latent from the current audit. The next GPU
  pass completed as a separate held-out prompt-end model-direction test;
  its results are in ../grouped_600_results/GROUPED_600_FINDINGS.txt. Causal steering still needs
  new prompt groups, warranted-agreement controls, and a clear free-generation
  task format before a "base knows how; chat learns when" claim.
