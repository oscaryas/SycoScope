Frozen-SAE decision-state diagnostic, fixed before paired extraction results

Extract the exact same four valid no-system verdict formats for 150 fixed
natural AITA posts × neutral/right/wrong stance: 1,800 contexts per official
Llama-3 model. The base and chat checkpoints receive the same Instruct
tokenizer IDs. At hidden-state indices 14 and 23, save the last event-text
token wholly inside the event text (recorded as story_end) and the final
token of the "Verdict:" cue (pre_verdict). A BPE token may combine final
punctuation with the following newline; record how often it crosses this
boundary and interpret story_end as the last *complete* story token, not
necessarily the final character of the story.
Match state metadata hashes to each other and to the previously scored
NTA/YTA candidate decisions. The no-example cue is excluded because greedy
verdict formatting was valid in only 146/450 chat and 53/450 base cases.

Locally apply the already trained chat SAEs without refitting: layer-14
n15/k3 from the teacher-forced pilot, layer-14 n40/k3 as a richer same-layer
check, and layer-23 n30/k3 as a later-layer check. Preserve each checkpoint's
Top-K rule and normalization. The SAEs were trained on *response-sentence
means*, not single decision tokens. Compare prompt-end activation strengths,
top-feature frequencies, and relative reconstruction error against the
lexicographically first 200 hashed old chat response-state files from the
600-conflict teacher-forced run (including their response-sentence means and
chat-template prompt-end states). Compare story_end with pre_verdict so AITA cue features are not
mistaken for epistemic gating. Also compare format-order sensitivity of each
feature code with the already observed verdict-order sensitivity.

Before looking at decisions, name the sentence-label features of interest:
layer-14 n40 latent 17 (validating reasonable behavior) and latent 16
(moral condemnation), versus style/cue controls n40 latent 39 (AITA verdict
query closer) and layer-14 n15 latent 14 (AITA verdict question closer).
Any activation/outcome association is exploratory because the 150 stories
and the formerly locked 30 have already been inspected for behavior, edited
claims lack independent human ratings, and sentence-level SAE training used
a different activation distribution. A feature must first survive this
distribution and cue audit, then independently rated new natural test data,
before intervention or causal-gate nomination. Applying a chat SAE directly
to base activations is descriptive only until base/chat feature alignment or
a separately trained base SAE is validated.
