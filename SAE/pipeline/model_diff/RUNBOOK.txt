Paired Llama-3 sycophancy pilot (2026-09-15)

Prepared inputs:
  SAE/results/model_diff/pilot_manifest.jsonl
  8 AITA-NTA-FLIP conflict pairs: 4 NTA/NTA, 4 mixed verdict
  16 existing sample-0 natural chat responses, checked against
  SAE/results/AITA-NTA-FLIP.jsonl (16/16 exact text matches)
  SAE/results/trained_sae/L14_n15_k3_uncentered_s0/{config.json,sae.pt}

Local code and preflight:
  python3 -B SAE/pipeline/model_diff/test_paired_teacher_force.py
  The pilot has 139-895 full-context tokens per response; no 4096-token skips.
  The SAE encoder reproduces TopKSAE.encode exactly on a random smoke input.

GPU host: ssh -p 22933 -i ~/.ssh/vast_vla root@82.66.51.122
  NVIDIA A100-SXM4 80GB; /venv/main Python has CUDA torch 2.8.0,
  transformers 4.56.2, spaCy 3.8.7, accelerate 1.10.1.
  /workspace is not persistent; the 32GB disk initially had ~26GB free
  after packages, so the two 16GB checkpoints were staged sequentially.

Completed GPU pilot:
  The user explicitly approved the official chat and base weight staging.
  Chat weights were uploaded/resumed through signed official CDN URLs, run,
  then only the copied large chat shards were removed to fit the base.
  Base shards were downloaded through short-lived signed URLs; no local
  Hugging Face login token was copied to the host.
  16/16 base/chat teacher-forced response pairs match token IDs and spans.
  The compact outputs and state arrays are back under
  SAE/results/model_diff/pilot_run/. See PILOT_FINDINGS.txt there.
  Three no-system base free-generation formats were also run and copied back.

Staged on the host under /workspace/syco_pilot:
  paired_teacher_force.py, pilot_manifest.jsonl, sae/config.json, sae/sae.pt.
  All four SHA-256 hashes match the local files; the remote script loads
  16 rows and the layer-14, 15-latent SAE.

Reproduction sequence used after explicit approval:
  1. Stage Meta-Llama-3-8B-Instruct tokenizer/weights on the host.
  2. Run the chat pass:
     /venv/main/bin/python paired_teacher_force.py extract
       --manifest pilot_manifest.jsonl --model-path chat_model
       --tokenizer-path chat_model --model-kind chat
       --out-dir results --sae-dir sae
  3. Free the chat weights, stage official Meta-Llama-3-8B base weights,
     and run the base pass with the *same* chat tokenizer path:
     /venv/main/bin/python paired_teacher_force.py extract
       --manifest pilot_manifest.jsonl --model-path base_model
       --tokenizer-path chat_model --model-kind base
       --out-dir results
  4. Run summarize on results/{chat,base}.jsonl. It refuses
     response pairs with different token IDs, lengths, or start offsets.
     Copy the compact results back and expand only after pilot inspection.

No causal mechanism claim follows from this pilot. It stores per-sentence,
pre-response, and early-response layer-14 states for later vector fitting.
The matched log-probability comparison is descriptive; fresh conflict groups,
held-out behavior tests, and timed steering are required for the full plan.

The user explicitly approved transferring the pilot prompt/response manifest,
SAE checkpoint, and subsequently both model checkpoints. Those transfers and
GPU passes succeeded. The host has no Hugging Face credential; the signed
download method avoided leaving the local login token on the instance.

Fresh first-verdict probe completed (2026-09-15)
  Local inputs: SAE/results/model_diff/decision_probe/fresh_scenarios.jsonl
  and fresh_manifest.jsonl. The latter has 96 prompts from 24 authored
  conflicts, each with actor/recipient perspective and right/wrong stance.
  Remote inputs are at /workspace/syco_pilot/{fresh_manifest.jsonl,
  score_verdict_decisions.py}; manifest SHA-256 is
  a7a01809652b577a14af1ed253bd32c8fb006c5953274d5c31ccd700ed82661b.
  Both official passes completed all 192 prompt/order cases and were copied
  to SAE/results/model_diff/decision_probe/{chat,base}.jsonl. All 192
  prompt-token and verdict-candidate hashes match across models; every
  greedy completion begins with NTA or YTA. Chat and base output SHA-256:
  25c587ec32164bb9d5e8f7cb65c1f97b3d0b91ed3f6520fb9cf4dbfbf64446ff;
  428d18ede79abd8b9bae136a88a248e0fdece900a7e57cf96601cddb3f211fa5.
  Large chat weight shards were removed after checksum verification to fit
  the base checkpoint; tokenizer/config/index remain on host.

  The user supplied an existing workspace .env containing HF_TOKEN. It was
  loaded only into the local staging Python process; the token and the
  other .env variables were never transferred or printed. For reproduction:
    python3 SAE/pipeline/model_diff/stage_official_checkpoint.py stage
      --repo meta-llama/Meta-Llama-3-8B --weights-only
      --dest /workspace/syco_pilot/base_model --env-file .env
  On the host, from /workspace/syco_pilot:
    /venv/main/bin/python score_verdict_decisions.py
      --manifest fresh_manifest.jsonl --model-path base_model
      --tokenizer-path chat_model --model-kind base
      --out decision_base.jsonl --resume
  With both results in the local decision_probe directory, run:
    .venv/bin/python SAE/pipeline/model_diff/analyze_decision_probe.py
      --chat SAE/results/model_diff/decision_probe/chat.jsonl
      --base SAE/results/model_diff/decision_probe/base.jsonl
      --out SAE/results/model_diff/decision_probe/paired_audit.json
  The analysis refuses base/chat prompt-token or candidate-token mismatches.
  Report both example orders, each narrator side, and cluster intervals;
  do not collapse the large example-order effect into a model claim.
  The results and limitations are in DECISION_PROBE_FINDINGS.txt.
